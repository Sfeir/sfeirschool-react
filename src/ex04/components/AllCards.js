import React from "react";

export function AllCards() {
  return (
    <main>
      <ul>
        <li>
          Display the list of all the people using the PersonCard component.
        </li>
        <li> What if a person has no manager?</li>
        <li>
          Switch between AllCards and Carousel using the button in the TopAppBar
        </li>
      </ul>
    </main>
  );
}
